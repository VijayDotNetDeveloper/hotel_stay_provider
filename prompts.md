# Prompts

- Generated backend project and APIs for Hotel Stay Aggregator.
- Implemented `IHotelProvider` with `PremierStaysProvider` and `BudgetNestsProvider`.
- Added date validation, missing parameter checks, and document mismatch handling.
- Created React frontend with search, results, reservation, and confirmation flows.
- Centralized API calls in `src/services/api.ts`.
- Added NUnit tests for provider normalization, filtering, and API validation.
